Git commands

1) git help <any git command>
        -- git help config
2) git config --global user.name "golam chowdhury"
   git config --global user.email adnan333bd@gmail.com
   git config --global color.ui true

3) git init
    creates local repo
4) git status
    what is changed after last commit
5) git add <filename>
    for start tracking on the file, or to add changes to staging
6) 


